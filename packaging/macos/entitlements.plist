<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!--
    The hardened runtime is required for notarisation, and it blocks a few things by default that
    a self-contained .NET application needs.
  -->

  <!-- .NET's JIT writes executable pages at runtime. -->
  <key>com.apple.security.cs.allow-jit</key>                        <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key> <true/>

  <!-- Loading the app's own native libraries, which are signed with the same identity. -->
  <key>com.apple.security.cs.disable-library-validation</key>       <true/>

  <!-- Cloudict launches Chrome and its driver as child processes. -->
  <key>com.apple.security.cs.allow-dyld-environment-variables</key> <true/>

  <key>com.apple.security.device.audio-input</key>                  <true/>
  <key>com.apple.security.network.client</key>                      <true/>
</dict>
</plist>
