# ---- Build output ----
[Bb]in/
[Oo]bj/
[Oo]ut/
publish/
*.binlog

# ---- Visual Studio / Rider ----
.vs/
.idea/
*.user
*.suo
*.userprefs

# ---- App runtime / user-generated files ----
settings.json
settings.backup.json
license.dat
*.log

# ---- Legacy ChromeDriver download cache (superseded by src/Cloudict/Drivers/) ----
# The driver Cloudict ships lives in src/Cloudict/Drivers/ and IS tracked, so the installer
# can work offline. Only the old runtime-download cache is ignored.
**/Chrome/

# ---- Internal localization tooling / temp test output ----
_loctools/
_run/
last-answer.md

# ---- Installer build output ----
installer/Output/

# ---- OS noise ----
Thumbs.db
.DS_Store
